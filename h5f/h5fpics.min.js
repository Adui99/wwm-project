(function webpackUniversalModuleDefinition(root, factory) {
	if(typeof exports === 'object' && typeof module === 'object')
		module.exports = factory();
	else if(typeof define === 'function' && define.amd)
		define([], factory);
	else {
		var a = factory();
		for(var i in a) (typeof exports === 'object' ? exports : root)[i] = a[i];
	}
})(window, function() {
return /******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, { enumerable: true, get: getter });
/******/ 		}
/******/ 	};
/******/
/******/ 	// define __esModule on exports
/******/ 	__webpack_require__.r = function(exports) {
/******/ 		if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 			Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 		}
/******/ 		Object.defineProperty(exports, '__esModule', { value: true });
/******/ 	};
/******/
/******/ 	// create a fake namespace object
/******/ 	// mode & 1: value is a module id, require it
/******/ 	// mode & 2: merge all properties of value into the ns
/******/ 	// mode & 4: return value when already ns object
/******/ 	// mode & 8|1: behave like require
/******/ 	__webpack_require__.t = function(value, mode) {
/******/ 		if(mode & 1) value = __webpack_require__(value);
/******/ 		if(mode & 8) return value;
/******/ 		if((mode & 4) && typeof value === 'object' && value && value.__esModule) return value;
/******/ 		var ns = Object.create(null);
/******/ 		__webpack_require__.r(ns);
/******/ 		Object.defineProperty(ns, 'default', { enumerable: true, value: value });
/******/ 		if(mode & 2 && typeof value != 'string') for(var key in value) __webpack_require__.d(ns, key, function(key) { return value[key]; }.bind(null, key));
/******/ 		return ns;
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = 0);
/******/ })
/************************************************************************/
/******/ ([
/* 0 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
// ESM COMPAT FLAG
__webpack_require__.r(__webpack_exports__);

// EXPORTS
__webpack_require__.d(__webpack_exports__, "picH5f", function() { return /* binding */ src_picH5f; });

// CONCATENATED MODULE: ./src/render.js
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }
function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, _toPropertyKey(descriptor.key), descriptor); } }
function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }
function _toPropertyKey(arg) { var key = _toPrimitive(arg, "string"); return typeof key === "symbol" ? key : String(key); }
function _toPrimitive(input, hint) { if (typeof input !== "object" || input === null) return input; var prim = input[Symbol.toPrimitive]; if (prim !== undefined) { var res = prim.call(input, hint || "default"); if (typeof res !== "object") return res; throw new TypeError("@@toPrimitive must return a primitive value."); } return (hint === "string" ? String : Number)(input); }
var render = /*#__PURE__*/function () {
  function render(cvs, cache) {
    _classCallCheck(this, render);
    var _self = this;
    this.cache = cache;
    this.view = cvs;
    this.nowframe = -1;
    this.ctx = cvs.getContext("2d");
    this.imgCache = {};
    this.img = new Image();
    this.img.onload = function () {
      _self.ctx.clearRect(0, 0, _self.view.width, _self.view.height);
      _self.ctx.drawImage(_self.img, 0, 0);
    };
  }
  _createClass(render, [{
    key: "init",
    value: function init(width, height) {
      this.width = width;
      this.height = height;
      this.view.width = width;
      this.view.height = height;
    }
  }, {
    key: "clear",
    value: function clear() {}
  }, {
    key: "display",
    value: function display(pic, frame) {
      var _self = this;
      this.nowframe = frame;
      if (this.cache) {
        if (this.imgCache[frame]) {
          this.ctx.clearRect(0, 0, this.view.width, this.view.height);
          this.ctx.drawImage(this.imgCache[frame], 0, 0);
        } else {
          var img = new Image();
          img.onload = function () {
            if (_self.nowframe == frame) {
              _self.ctx.clearRect(0, 0, _self.view.width, _self.view.height);
              _self.ctx.drawImage(img, 0, 0);
            }
            _self.imgCache[frame] = img;
          };
          img.src = pic;
        }
      } else {
        this.img.src = pic;
      }
    }
  }]);
  return render;
}();

// CONCATENATED MODULE: ./src/load.js
function load_classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }
function load_defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, load_toPropertyKey(descriptor.key), descriptor); } }
function load_createClass(Constructor, protoProps, staticProps) { if (protoProps) load_defineProperties(Constructor.prototype, protoProps); if (staticProps) load_defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }
function load_toPropertyKey(arg) { var key = load_toPrimitive(arg, "string"); return typeof key === "symbol" ? key : String(key); }
function load_toPrimitive(input, hint) { if (typeof input !== "object" || input === null) return input; var prim = input[Symbol.toPrimitive]; if (prim !== undefined) { var res = prim.call(input, hint || "default"); if (typeof res !== "object") return res; throw new TypeError("@@toPrimitive must return a primitive value."); } return (hint === "string" ? String : Number)(input); }
var load = /*#__PURE__*/function () {
  function load() {
    load_classCallCheck(this, load);
    this.reset();
  }
  load_createClass(load, [{
    key: "reset",
    value: function reset() {
      this.url = "";
      this.streaming = false;
      this.completed = false;
      this.established = false;
      this.progress = 0;
      this.fileSize = 0;
      this.loadedSize = 0;
      this.chunkSize = 1024 * 1024;
      this.isLoading = false;
      this.loadStartTime = 0;
      this.aborted = false;
      this.onResponse = null;
      this.onChunkData = null;
      this.completedFun = null;
    }
  }, {
    key: "start",
    value: function start(url, respcbfun, pgcbfun, okcbfun) {
      this.onResponse = respcbfun;
      this.onChunkData = pgcbfun;
      this.completedFun = okcbfun;
      this.url = url;
      this.request = new XMLHttpRequest();
      this.request.onreadystatechange = function () {
        if (this.request.readyState === this.request.DONE) {
          this.fileSize = parseInt(this.request.getResponseHeader("Content-Length"));
          if (this.onResponse) {
            this.onResponse(this);
          }
          this.loadNextChunk();
        }
      }.bind(this);
      this.request.open('HEAD', this.url);
      this.request.send();
    }
  }, {
    key: "loadNextChunk",
    value: function loadNextChunk() {
      var start = this.loadedSize;
      var end = Math.min(this.loadedSize + this.chunkSize - 1, this.fileSize - 1);
      if (start >= this.fileSize || this.aborted) {
        this.completed = true;
        if (this.completedFun) {
          this.completedFun();
        }
        return;
      }
      this.isLoading = true;
      this.loadStartTime = +new Date();
      this.request = new XMLHttpRequest();
      this.request.onreadystatechange = function () {
        if (this.request.readyState === this.request.DONE && this.request.status >= 200 && this.request.status < 300) {
          this.onChunkLoad(this.request.response);
        } else if (this.request.readyState === this.request.DONE) {
          if (this.loadFails++ < 3) {
            this.loadNextChunk();
          }
        }
      }.bind(this);
      this.request.open('GET', this.url + '?' + start + "-" + end);
      this.request.setRequestHeader("Range", "bytes=" + start + "-" + end);
      this.request.responseType = "arraybuffer";
      this.request.send();
    }
  }, {
    key: "onChunkLoad",
    value: function onChunkLoad(data) {
      this.established = true;
      this.progress = 1;
      this.loadedSize += data.byteLength;
      this.loadFails = 0;
      this.isLoading = false;
      if (this.onChunkData) {
        this.onChunkData(data);
      }
      this.loadTime = +new Date() - this.loadStartTime;
      this.loadNextChunk();
    }
  }, {
    key: "destroy",
    value: function destroy() {
      if (this.request) {
        this.request.abort();
      }
      this.aborted = true;
    }
  }]);
  return load;
}();

// CONCATENATED MODULE: ./src/index.js
function src_classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }
function src_defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, src_toPropertyKey(descriptor.key), descriptor); } }
function src_createClass(Constructor, protoProps, staticProps) { if (protoProps) src_defineProperties(Constructor.prototype, protoProps); if (staticProps) src_defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }
function src_toPropertyKey(arg) { var key = src_toPrimitive(arg, "string"); return typeof key === "symbol" ? key : String(key); }
function src_toPrimitive(input, hint) { if (typeof input !== "object" || input === null) return input; var prim = input[Symbol.toPrimitive]; if (prim !== undefined) { var res = prim.call(input, hint || "default"); if (typeof res !== "object") return res; throw new TypeError("@@toPrimitive must return a primitive value."); } return (hint === "string" ? String : Number)(input); }


var _events = new WeakMap();
var src_picH5f = /*#__PURE__*/function () {
  function picH5f(opts) {
    src_classCallCheck(this, picH5f);
    _events.set(this, {});
    var _self = this;
    var _self = this;
    if (!opts) {
      opts = {};
    }
    if (opts.container) {
      var cid = opts.container;
      var obj = document.getElementById(cid);
      if (obj.tagName.toLowerCase() == 'canvas') {
        this.view = obj;
      } else {
        this.viewCon = obj;
        this.view = document.createElement('canvas');
        this.viewCon.appendChild(this.view);
      }
    } else {
      this.view = document.createElement('canvas');
    }
    if (opts.autoPlay) {
      this.autoPlay = true;
    } else {
      this.autoPlay = false;
    }
    if (opts.loop) {
      this.loop = true;
    } else {
      this.loop = false;
    }
    if (opts.cache) {
      this.cache = true;
    } else {
      this.cache = false;
    }
    this.det = false;
    this.canPlay = false;
    this.needResume = false;
    this.canPlayEvent = false;
    this.onPause = false;
    this.loadok = false;
    this.width = 0;
    this.height = 0;
    this.currentFrame = 0;
    this.fileid = 0;
    this.nowRenderFrame = -1;
    this.totalFrame = 0;
    this.framerate = 0;
    this.currentTime = 0;
    this.lastFrame = -1;
    this.lastTime = 0;
    this.dataOfs = 0;
    this.bufferLen = 0;
    this.frameBuffer = [];
    this.frameSize = [];
    this.dataNum = 0;
    this.cacheNum = 0;
    this.render = new render(this.view, this.cache);
    this.loadObj = new load();
    var hidden, visibilityChange;
    if (typeof document.hidden !== "undefined") {
      hidden = "hidden";
      visibilityChange = "visibilitychange";
    } else if (typeof document.msHidden !== "undefined") {
      hidden = "msHidden";
      visibilityChange = "msvisibilitychange";
    } else if (typeof document.webkitHidden !== "undefined") {
      hidden = "webkitHidden";
      visibilityChange = "webkitvisibilitychange";
    }
    function handleVisibilityChange() {
      if (document[hidden]) {
        _self.visibleChg(false);
      } else {
        _self.visibleChg(true);
      }
    }
    if (typeof document.addEventListener === "undefined" || typeof document[hidden] === "undefined") {} else {
      document.addEventListener(visibilityChange, handleVisibilityChange, false);
    }
  }
  src_createClass(picH5f, [{
    key: "addEventListener",
    value: function addEventListener(type, listener) {
      var options = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};
      var events = _events.get(this);
      if (!events) {
        events = {};
        _events.set(this, events);
      }
      if (!events[type]) {
        events[type] = [];
      }
      events[type].push(listener);
    }
  }, {
    key: "removeEventListener",
    value: function removeEventListener(type, listener) {
      var events = _events.get(this);
      if (events) {
        var listeners = events[type];
        if (listeners && listeners.length > 0) {
          for (var i = listeners.length; i--; i > 0) {
            if (listeners[i] === listener) {
              listeners.splice(i, 1);
              break;
            }
          }
        }
      }
    }
  }, {
    key: "dispatchEvent",
    value: function dispatchEvent() {
      var event = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
      var listeners = _events.get(this)[event.type];
      if (listeners) {
        for (var i = 0; i < listeners.length; i++) {
          listeners[i](event);
        }
      }
    }
  }, {
    key: "src",
    get: function get() {
      return this._src;
    },
    set: function set(val) {
      if (this._src) {
        this.pause();
        this.clean();
      }
      this._src = val;
      this.fileid++;
      this.load();
    }
  }, {
    key: "visibleChg",
    value: function visibleChg(sta) {
      if (sta) {
        if (this.needResume) {
          this.needResume = false;
          this.play();
        }
      } else {
        if (this.onPlay) {
          this.needResume = true;
          this.pause();
        }
      }
    }
  }, {
    key: "playOver",
    value: function playOver() {
      this.onPlay = false;
      this.dispatchEvent({
        type: 'ended'
      });
      if (this.loop) {
        this.play();
      }
    }
  }, {
    key: "reset",
    value: function reset() {
      this.currentFrame = 0;
      this.lastFrame = -1;
      this.currentTime = 0;
      this.lastTime = +new Date();
      this.onPlay = false;
    }
  }, {
    key: "pause",
    value: function pause() {
      this.onPause = true;
      this.onPlay = false;
    }
  }, {
    key: "clean",
    value: function clean() {
      this.reset();
      this.det = false;
      this.canPlay = false;
      this.canPlayEvent = false;
      this.onPause = false;
      this.loadok = false;
      this.width = 0;
      this.height = 0;
      this.currentFrame = 0;
      this.fileid = 0;
      this.nowRenderFrame = -1;
      this.totalFrame = 1;
      this.framerate = 0;
      this.currentTime = 0;
      this.lastFrame = -1;
      this.lastTime = 0;
      this.dataOfs = 0;
      this.bufferLen = 0;
      this.frameBuffer = [];
      this.frameSize = [];
      this.dataNum = 0;
      this.cacheNum = 0;
    }
  }, {
    key: "play",
    value: function play() {
      if (this.canPlay) {
        this.onPause = false;
        if (this.currentFrame == this.totalFrame - 1) {
          this.currentFrame = 0;
        }
        this.loopPlay();
      } else {
        this.autoPlay = true;
      }
    }
  }, {
    key: "loopPlay",
    value: function loopPlay() {
      var _this = this;
      if (this.onPause) {
        return;
      }
      if (this.uto) {
        clearTimeout(this.uto);
      }
      this.onPlay = true;
      this.uto = setTimeout(function () {
        _this.loopPlay();
      }, 1000 / this.framerate);
      this.renderFrame();
    }
  }, {
    key: "renderFrame",
    value: function renderFrame() {
      if (this.currentFrame < 0) {
        this.currentFrame = 0;
      }
      if (this.currentFrame > this.dataNum - 1) {
        return;
      }
      this.lastTime = +new Date();
      this.render.display(this.frameBuffer[this.currentFrame].pic, this.currentFrame);
      this.dispatchEvent({
        type: 'frame'
      });
      this.currentFrame++;
      if (this.currentFrame >= this.totalFrame) {
        this.playOver();
        if (!this.loop) {
          if (this.uto) {
            clearTimeout(this.uto);
          }
          this.onPause = true;
          this.currentFrame = this.totalFrame - 1;
          return;
        }
      }
      if (this.currentFrame >= this.totalFrame) {
        this.currentFrame = 0;
      }
    }
  }, {
    key: "gotoAndPlay",
    value: function gotoAndPlay(num) {
      this.onPause = false;
      this.currentFrame = num;
      this.lastTime = +new Date();
      this.renderFrame();
      this.loopPlay();
    }
  }, {
    key: "gotoAndStop",
    value: function gotoAndStop(num) {
      this.onPause = true;
      this.currentFrame = num;
      this.renderFrame();
    }
  }, {
    key: "load",
    value: function load() {
      var _self = this;
      if (!this._src) {
        return;
      }
      this.clean();
      this.loadObj.destroy();
      this.loadObj.reset();
      var fsize, fdata, fview;
      this.loadObj.start(this._src, function (res) {
        fsize = res.fileSize;
        fdata = new Uint8Array(fsize);
        fview = new DataView(fdata.buffer);
      }, function (data) {
        var udata = new Uint8Array(data);
        fdata.set(udata, _self.bufferLen);
        _self.bufferLen += data.byteLength;
        if (!_self.det) {
          _self.det = true;
          var tag = String.fromCharCode(fview.getUint8(0)) + String.fromCharCode(fview.getUint8(1)) + String.fromCharCode(fview.getUint8(2));
          if (tag != 'an2') {
            throw new Error("not a pic frame file");
          }
          _self.framerate = fview.getInt32(3, true);
          _self.totalFrame = fview.getInt32(7, true);
          _self.height = fview.getInt32(11, true);
          _self.width = fview.getInt32(15, true);
          _self.render.init(_self.width, _self.height);
          var ofs = 15;
          for (var i = 0; i < _self.totalFrame; i++) {
            _self.frameSize.push(fview.getInt32(ofs + 4, true));
            ofs += 4;
          }
          _self.dataOfs = ofs + 4;
        }
        while (_self.dataNum < _self.totalFrame) {
          var fsize1 = _self.frameSize[_self.dataNum];
          if (_self.bufferLen < _self.dataOfs + fsize1) {
            break;
          }
          var ofs = _self.dataOfs;
          var sdata = fdata.subarray(ofs, ofs + fsize1);
          var bdata = new Uint8Array(fsize1);
          bdata.set(sdata);
          var blob = new Blob([bdata]);
          var purl = URL.createObjectURL(blob);
          _self.frameBuffer.push({
            num: _self.dataNum,
            pic: purl
          });
          _self.dataOfs += fsize1;
          _self.dataNum++;
          if (!_self.canPlay) {
            _self.canPlay = true;
            if (_self.autoPlay) {
              _self.onPause = false;
              _self.lastTime = 0;
              _self.play();
            } else {
              _self.render.display(_self.frameBuffer[0].pic);
            }
          }
        }
      }, function () {
        _self.loadok = true;
        _self.dispatchEvent({
          type: 'loadok'
        });
        fdata = null;
        fview = null;
      });
    }
  }]);
  return picH5f;
}();

/***/ })
/******/ ]);
});